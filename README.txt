Rename loaderconfig-DEVICE.h to loaderconfig.h for everything to work.
-usbasp is configured for USBasp programmer pins,
-default is for OBDev.at defaults (D-/D+ on D0/D2, no pullup).

Copy the -default header and modify it if you need a custom setup.
